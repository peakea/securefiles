<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Download File - SecureFiles</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
        }
        h1 {
            color: #333;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            font-size: 18px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            letter-spacing: 4px;
            text-align: center;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
        }
        button:hover {
            background-color: #45a049;
        }
        .error {
            padding: 15px;
            background-color: #ffebee;
            border-left: 4px solid #f44336;
            color: #c62828;
            margin-bottom: 20px;
            border-radius: 4px;
        }
        .info {
            margin-top: 20px;
            padding: 15px;
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
            border-radius: 4px;
        }
        .uuid-display {
            background-color: #f0f0f0;
            padding: 10px;
            border-radius: 4px;
            font-family: monospace;
            word-break: break-all;
            margin-bottom: 20px;
        }
        a {
            color: #2196F3;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Download File</h1>
    
    <p>Enter the TOTP code to download your file.</p>
    
    <% if (error) { %>
        <div class="error">
            <strong>Error:</strong> <%= error %>
        </div>
    <% } %>
    
    <div class="uuid-display">
        <strong>File UUID:</strong><br>
        <%= uuid %>
    </div>
    
    <form action="/download/<%= uuid %>" method="POST">
        <div class="form-group">
            <label for="totp">TOTP Code (6 digits):</label>
            <input 
                type="text" 
                id="totp" 
                name="totp" 
                placeholder="000000" 
                required 
                maxlength="6" 
                pattern="[0-9]{6}"
                inputmode="numeric"
            >
        </div>
        
        <button type="submit">Download File</button>
    </form>
    
    <div class="info">
        <strong>Note:</strong> TOTP codes change every 30 seconds. If your code doesn't work, 
        generate a new one from your authenticator app and try again.
    </div>
    
    <hr>
    
    <p>
        <a href="/">← Back to Home</a> | 
        <a href="/totp-test">Test TOTP Generator</a>
    </p>
</body>
</html>
